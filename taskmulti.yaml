version: v1.0.0
steps:
# Build target image
- build: -t {{.Run.Registry}}/appimages/sonarqube:{{.Run.ID}} -f docker-sonarqube/7/community/Dockerfile --build-arg REGISTRY_NAME={{.Run.Registry}}.azurecr.io .
  disableWorkingDirectoryOverride: true
# Push image
- push:
  - {{.Run.Registry}}/appimages/sonarqube:{{.Run.ID}}
